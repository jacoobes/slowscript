class Test {

    object(name, grade) {
        instance.name = name
        instance.grade = grade
    }



}

class Quiz from Test {
    object(name, grade, bob) {
        instance.name = name
        instance.grade = grade
        instance.bob = bob
    }

}

Quiz("Math" , "grade", "no")
